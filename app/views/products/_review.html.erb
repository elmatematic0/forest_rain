<div>
  <h4>Review by <%= review.author_name %></h4>
  <p class="review"><%= review.body %></p>
  <p>Posted <%= distance_of_time_in_words(review.product.created_at, review.created_at) %> later</p>
</div>
